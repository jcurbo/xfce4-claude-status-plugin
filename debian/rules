#!/usr/bin/make -f

%:
	dh $@

override_dh_auto_build:
	$(MAKE)

override_dh_auto_install:
	$(MAKE) install DESTDIR=$(CURDIR)/debian/xfce4-claude-status-plugin

override_dh_auto_clean:
	$(MAKE) clean

override_dh_auto_test:
	# Skip tests - cppcheck is for local development only
